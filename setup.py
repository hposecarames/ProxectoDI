from setuptools import setup

setup(
   name='ProxectoDI',
   version='1.0',
   description='Aplicación gestión',
   author='Hector Pose Carames',
   author_email='hposecarames@danielcastelao.org',
   packages=['login', 'menu', 'baseDatos', 'formularios', 'facturas'],  #same as name
   install_requires=[],
)
